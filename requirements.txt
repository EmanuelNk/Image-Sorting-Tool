PyQt6>=6.4.0
Pillow>=10.0.0
pillow-heif>=0.13.0
rawpy>=0.18.0      # RAW support — requires: brew install libraw
pyobjc-framework-Quartz>=10.0 ; sys_platform == "darwin"   # fast HEIC decode via Apple ImageIO (macOS only)
